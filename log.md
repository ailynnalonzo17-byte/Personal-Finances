# Log

- 2026-09-16: First Vera session. No Memory Vault or Airtable routines/tasks/skills tables existed
  yet. Confirmed this is personal finance, not a landlord business — repurposed Tessa (tenants →
  correspondence) and Owen (property → operations/filing), kept Fiona as money but adapted from
  rent/tenants to bills/cards/loans. Set up repo layout, business profile (from repo data, no
  interview run), CLAUDE.md, and the team under `.claude/skills/`. Added Routines, Tasks and Skills
  tables to the Personal Finance Airtable base and logged the four skills there.
- 2026-09-17: Found five stranded session branches from earlier runs that had never reached `main`.
  Consolidated the real work into this session's branch: the Memory Vault setup (repo layout,
  business profile, the team), the dashboard's cache/coalesce/cleared-entry work, and
  `scripts/due_today.py`. One branch (`claude/brave-goldberg-5pgq6t`) was an earlier, smaller
  attempt at the same Memory Vault setup, fully superseded by the later one — dropped, nothing
  unique in it. Pushed to `claude/trusting-ptolemy-too43c`; the five old branches on GitHub still
  need deleting (a destructive push was blocked by this session's permissions) and this branch
  still needs merging into `main`.
